# Day 45 - Context API

- Avoids prop drilling
- Global state sharing
- createContext + Provider + useContext

Flow:
Provider → wrap App
useContext → access data

Use cases:
- Auth
- Theme
- Shared state