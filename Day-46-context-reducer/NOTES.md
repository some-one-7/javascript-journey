# Day 46 - Context + useReducer

- useReducer for complex logic
- dispatch actions instead of setState
- central state management

Flow:
dispatch → reducer → new state → UI update

Benefits:
- predictable state
- scalable
- clean logic separation